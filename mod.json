{
	"geode": "5.5.3",
	"gd": {
		"win": "2.2081",
		"android": "2.2081",
		"mac": "2.2081",
		"ios": "2.2081"
	},
	"id": "imes.whatishedoing",
	"name": "What is he doing?",
	"version": "1.0.0",
	"developer": "imes",
	"description": "Discord webhook notifications for Geometry Dash.",
	"dependencies": {
		"geode.node-ids": ">=v1.20.0"
	},
	"links": {
		"community": "https://discord.gg/E8f6D6XqbW"
	},
	"tags": [
		"online",
		"joke"
	],
	"settings": {
		"webhook-title": {
			"type": "title",
			"name": "Discord Webhook"
		},
		"webhook-url": {
			"type": "string",
			"name": "Webhook URL",
			"description": "Your Discord webhook URL. Leave empty to disable.",
			"default": "",
			"filter": "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789:/.?=-_"
		},
		"webhook-username": {
			"type": "string",
			"name": "Webhook Display Name",
			"description": "The name shown as the sender in Discord.",
			"default": "What is he doing?"
		},
		"player-name": {
			"type": "string",
			"name": "Player Name",
			"description": "Subject of each Discord message (e.g., 'imes opened Geometry Dash!'). Empty defaults to 'He'.",
			"default": "He"
		},
		"notify-title": {
			"type": "title",
			"name": "Notification Toggles"
		},
		"notify-game-open": {
			"type": "bool",
			"name": "Notify on Game Open",
			"description": "When the game is opened (main menu) and when Geometry Dash is closed (includes session length).",
			"default": true
		},
		"notify-play-level": {
			"type": "bool",
			"name": "Notify on Playing a Level",
			"description": "When starting a level (level name, creator, ID) and when quitting a level (includes time in that level).",
			"default": true
		},
		"notify-level-complete": {
			"type": "bool",
			"name": "Notify on Level Complete",
			"description": "When a level is beaten (includes level info and total time spent on that run).",
			"default": true
		},
		"notify-editor": {
			"type": "bool",
			"name": "Notify on Opening the Editor",
			"description": "When opening the level editor and when leaving it (save & exit, exit, or exit without save), with session length.",
			"default": true
		}
	}
}